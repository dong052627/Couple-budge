{
  "entities": {
    "user": {
      "title": "User Profile",
      "description": "Information about a user, including their name, email, avatar, and pairing state.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The unique user ID from Firebase Auth." },
        "displayName": { "type": "string", "description": "The Google account identity display name." },
        "photoURL": { "type": "string", "format": "uri", "description": "The user avatar URL." },
        "email": { "type": "string", "format": "email", "description": "The user email." },
        "myCode": { "type": "string", "description": "The unique uppercase invitation code generated for this user." },
        "partnerCode": { "type": "string", "description": "The invitation code of the partner they entered." },
        "partnerUid": { "type": "string", "description": "The UID of the successfully paired partner." },
        "partnerName": { "type": "string", "description": "The display name of the paired partner." },
        "status": { "type": "string", "enum": ["unbound", "binding", "bound"], "description": "Matching state." },
        "spaceId": { "type": "string", "description": "Shared ledger space ID, computed symmetrically from sorted UIDs." },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Timestamp of last status modification." }
      },
      "required": ["uid", "displayName", "email", "myCode", "status"]
    },
    "expense": {
      "title": "Expense Item",
      "description": "An expense item uploaded into the shared ledger space.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Symmetrical primary unique ID for the transaction." },
        "spaceId": { "type": "string", "description": "Unique shared ID identifying the couple namespace." },
        "category": { "type": "string", "description": "Expense category key." },
        "amount": { "type": "number", "description": "Absolute invoice amount total." },
        "payerId": { "type": "string", "description": "UID of the payer." },
        "payerName": { "type": "string", "description": "Display name of the payer." },
        "date": { "type": "string", "format": "date", "description": "User local calendar spending date." },
        "note": { "type": "string", "description": "Descriptions or custom notes." },
        "splitType": { "type": "string", "enum": ["50/50", "single", "custom"], "description": "Splitting logic code." },
        "fullBearerId": { "type": "string", "description": "UID of full bearer if single bearer format." },
        "fullBearerName": { "type": "string", "description": "Name of full bearer if single bearer format." },
        "customShares": { "type": "object", "description": "Dictionary of UID to percentage mapping for custom shares." },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "spaceId", "category", "amount", "payerId", "payerName", "date", "splitType"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "user",
      "description": "User profile definitions."
    },
    "expenses/{expenseId}": {
      "schema": "expense",
      "description": "Shared ledger ledger expenses."
    }
  }
}
